# 🧪 Руководство по тестированию интеграции API чата

## Подготовка к тестированию

### 1. Убедиться что сервер запущен
```bash
# Проверить что API доступно
curl http://94.241.170.140:8080/api/ping
# Должен вернуть: {"message": "pong"}
```

### 2. Получить тестовый токен
```bash
# 1. Инициировать вход
curl -X POST http://94.241.170.140:8080/api/initlogin \
  -H "Content-Type: application/json" \
  -d '{"phone": "9001234567"}'

# 2. Получить код подтверждения из SMS/logs
# Код должен быть в формате XXXX

# 3. Подтвердить вход
curl -X POST http://94.241.170.140:8080/api/confirmlogin \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "9001234567",
    "code": "1234",
    "username": "testuser"
  }'

# Результат: {"token": "eyJhbGc..."}
```

## Тестирование REST API

### 1. Получить текущего пользователя
```bash
TOKEN="ваш_токен_здесь"

curl http://94.241.170.140:8080/api/user/me \
  -H "Authorization: Bearer $TOKEN"

# Ответ должен содержать ваш ID:
# {"id": 123, "username": "testuser", ...}
```

### 2. Получить список чатов
```bash
curl http://94.241.170.140:8080/api/chat/list \
  -H "Authorization: Bearer $TOKEN"

# Ответ:
# [{
#   "id": 1,
#   "name": "Chat Name",
#   "createdAt": "2023-10-18T...",
#   "lastMessage": {...}
# }]
```

### 3. Получить сообщения чата
```bash
CHAT_ID=1

curl "http://94.241.170.140:8080/api/chat/$CHAT_ID/messages?limit=20&direction=older" \
  -H "Authorization: Bearer $TOKEN"

# Ответ:
# {
#   "data": [
#     {
#       "id": 1,
#       "chatID": 1,
#       "senderID": 123,
#       "message": "Hello",
#       "createdAt": "2023-10-18T..."
#     }
#   ],
#   "pagination": {
#     "nextCursor": "...",
#     "hasNext": true,
#     "totalCount": 100
#   }
# }
```

## Тестирование WebSocket

### 1. Использовать wscat
```bash
npm install -g wscat

# Подключиться к WebSocket
wscat -c "ws://94.241.170.140:8080/api/chat/1/ws" \
  -H "Authorization: Bearer $TOKEN"

# Должны получить начальные события:
# {"type":"room_info","message":{...}}
# {"type":"history","messages":[...],"meta":{...}}
```

### 2. Отправить сообщение
```bash
# В wscat консоли выполнить:
> {"type":"message","message":"Hello World","timestamp":1634567890123}

# Должны получить подтверждение:
# {"type":"message_sent","message_id":12345,"timestamp":"2023-10-18T..."}
```

### 3. Отправить индикатор печати
```bash
> {"type":"typing","message":"true"}

# Ответ от других клиентов:
# {"type":"typing","user_id":123,"message":true,"chat_id":1}
```

### 4. Отметить как прочитано
```bash
> {"type":"read_receipt","message":"12345"}

# Подтверждение:
# {"type":"read_receipt","message_id":12345}
```

## Тестирование в приложении

### 1. Авторизация
- [ ] Открыть приложение
- [ ] Выполнить вход (SMS код)
- [ ] Убедиться что redirected на `/messages`
- [ ] Проверить что `credentials.userId` установлен

### 2. Список чатов
- [ ] Проверить что список загружается
- [ ] Должна появиться ActivityIndicator на время загрузки
- [ ] После загрузки должны видны список чатов
- [ ] Проверить что `lastMessage` и `lastTime` отображаются

### 3. Открытие диалога
- [ ] Кликнуть на чат из списка
- [ ] Должна загрузиться история сообщений
- [ ] Проверить что WebSocket подключается (в консоли: "WebSocket connected...")
- [ ] Должны видны все сообщения из истории

### 4. Отправка сообщения
- [ ] Набрать сообщение в TextInput
- [ ] Нажать кнопку send
- [ ] Проверить в консоли: "Message sent to chat..."
- [ ] Сообщение должно появиться в списке
- [ ] Проверить что `senderId === chatStore.currentUserId`

### 5. Получение новых сообщений
- [ ] Открыть второе соединение wscat в другом терминале
- [ ] Отправить сообщение через wscat
- [ ] Проверить что сообщение появилось в приложении
- [ ] Консоль должна показать: "WebSocket message for chat X: message"

### 6. Закрытие и переподключение
- [ ] Закрыть диалог (нажать back)
- [ ] Проверить в консоли: "Disconnected from chat..."
- [ ] Открыть тот же диалог снова
- [ ] Проверить что история загружается снова
- [ ] WebSocket переподключается

## Ошибки и их решение

### Ошибка: "WebSocket is not defined"
**Причина**: React Native Web не полностью поддерживает WebSocket  
**Решение**: Убедиться что используется Expo Web с правильными polyfill'ами

### Ошибка: 401 Unauthorized
**Причина**: Токен невалидный или истекшим  
**Решение**: Получить новый токен через confirmLogin

### Ошибка: 403 Forbidden
**Причина**: Пользователь не является членом чата  
**Решение**: Убедиться что пользователь добавлен в чат через API

### Ошибка: "Failed to parse WebSocket message"
**Причина**: Неправильный JSON формат от сервера  
**Решение**: Проверить в консоли полное сообщение, открыть issue

### WebSocket не получает сообщения
**Причина 1**: Соединение закрыто  
**Решение**: Проверить консоль на "WebSocket closed"

**Причина 2**: Firewall/proxy блокирует WebSocket  
**Решение**: Проверить сетевые настройки

## Monitoring и Debugging

### Консоль браузера (F12)
```
Logging для отладки:
- "Connecting to WebSocket: ws://..."
- "WebSocket connected to chat X"
- "WebSocket message for chat X: {type}"
- "Failed to parse WebSocket message: ..."
- "WebSocket error for chat X: ..."
- "Disconnected from chat X"
```

### Сетевой трафик (Network tab)
1. REST запросы должны быть в статусе 200
2. WebSocket должен быть в статусе 101 (Switching Protocols)
3. Проверить заголовок Authorization: Bearer

### Сохраненные сообщения
```typescript
// В консоли браузера выполнить:
import { chatStore } from "@/services/chat";
console.log(chatStore.messagesByDialog);
// Должен показать Map с сообщениями по dialogId
```

## Чек-лист перед деплоем

- [ ] Все REST API запросы возвращают 200/201
- [ ] WebSocket соединение устанавливается (101)
- [ ] Сообщения отправляются успешно
- [ ] Сообщения получаются в реальном времени
- [ ] История загружается корректно
- [ ] Ошибки обрабатываются и выводятся пользователю
- [ ] Нет unhandled promise rejections
- [ ] Нет WebSocket на странице после выхода из диалога
- [ ] TypeScript компилируется без ошибок в новых файлах
- [ ] Все импорты работают корректно

## Производительность

### Рекомендации
1. **Ограничить количество сообщений в памяти**: По умолчанию загружается 50
2. **Использовать пагинацию**: Для очень больших чатов
3. **Оптимизировать렌더링**: Используйте React.memo для MessageBubble
4. **Закрывать WebSocket**: При выходе из диалога

### Тестирование нагрузки
```bash
# Отправить 100 сообщений через wscat
for i in {1..100}; do 
  echo '{"type":"message","message":"Message '$i'","timestamp":'$(date +%s000)'}'; 
done | wscat -c "ws://..."
```

## Дополнительное тестирование

### Тест пагинации
```bash
# Получить первую страницу
curl "http://.../api/chat/1/messages?limit=20&direction=older"

# Использовать nextCursor для второй страницы
curl "http://.../api/chat/1/messages?cursor=abc123&limit=20&direction=older"
```

### Тест с несколькими пользователями
1. Создать два тестовых пользователя
2. Открыть один и тот же чат в обоих
3. Отправить сообщение из одного клиента
4. Проверить что другой клиент получит сообщение в реальном времени

### Тест при разных сетевых условиях
- Использовать DevTools > Network > Throttling
- Симулировать 3G, 4G, offline
- Проверить что ошибки обрабатываются корректно

---

**Статус**: Ready for testing  
**Дата**: 2024-12-10

